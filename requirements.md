# 要件定義 (Requirements)

本書は、本システムの「求める振る舞い・価値・制約・あるべき姿 (What/Why)」を定義する。
設計や実装の詳細 (How) は `design.md` に記述する。

## 運用方針

- 本書の各要件は、固定 ID を維持したまま「最新の正しい状態」に上書き更新する。
- 本書の `Fit Criteria` には、外部から観測可能な合否判定条件のみを記述する。
- 実装方法、内部データ表現、クラス名、関数名、CLI 引数名、既定値、バックエンド名は `design.md` に記述する。

## 1. マスク機能要件 (Mask Requirements)

### REQ-MASK-01: 動的領域を除外した学習
- **What**: 利用者は画像ごとのマスクを与え、指定領域を画像再構成の損失計算から除外した静的シーンの3D Gaussian Splattingモデルを学習できる。
- **Why**: 移動物体、人物、車両、撮影リグなどが静的シーン表現へ混入する影響を抑えるため。
- **Fit Criteria**:
  - マスクで除外された画素の予測値だけを変化させても、画像再構成の損失値が変化しないこと。
  - マスクで保持された画素は、画像再構成の損失値へ反映されること。
  - マスクを指定しない学習では、本家と同じ画素領域が損失計算の対象になること。

### REQ-MASK-02: 動的領域に由来する初期点の除外
- **What**: 利用者は、マスクで除外された領域にのみ観測される点を3DGSの初期点群から除外できる。
- **Why**: 動的物体に由来する初期Gaussianが学習開始時から静的シーンへ混入することを防ぐため。
- **Fit Criteria**:
  - 除外領域にしか有効な観測を持たない点が初期点群に含まれないこと。
  - 利用者が指定した最小数以上の保持領域の観測を持つ点が初期点群に含まれること。
  - 初期点の除外を無効化した場合、マスクによる点群の削減が行われないこと。

### REQ-MASK-03: 画像単位マスクの安全な受け入れ
- **What**: 利用者は元画像に対応するマスク画像の格納場所、前景の意味、二値化条件、欠損時の扱いを選択できる。
- **Why**: 異なるマスク生成ツールの出力を利用しつつ、対応漏れや意味の逆転を無音で学習へ混入させないため。
- **Fit Criteria**:
  - 元画像と同じ相対パス上の同名stemを持つマスクが対応付けられること。
  - 非ゼロ側を除外領域とする入力と、非ゼロ側を保持領域とする入力の双方を扱えること。
  - マスクの解像度が学習画像と異なる場合でも、領域境界を補間で混合せず画像座標へ対応付けられること。
  - 必須マスクが不足する場合は既定で学習開始前に失敗し、明示的に許可した場合のみ不足画像全体を保持領域として扱うこと。

### REQ-MASK-04: マスク領域に整合する評価
- **What**: マスクを利用する検証では、学習時と同じ保持領域だけを画質指標と可視化結果の対象にできる。
- **Why**: 学習対象外の動的領域によって静的シーンの品質評価が歪むことを防ぐため。
- **Fit Criteria**:
  - 除外画素の予測値だけを変化させても、マスク対応の画質指標が変化しないこと。
  - 保持画素が存在しない検証データだけが与えられた場合、成功値を生成せず明示的に失敗すること。

## 2. 導入要件 (Installation Requirements)

### REQ-INSTALL-01: 分離されたソースインストール
- **What**: 利用者は既存のPythonプロジェクトやグローバル環境を汚さず、本リポジトリ本体と実行例の依存関係を専用環境へ導入できる。
- **Why**: Python、PyTorch、CUDA、ビルドツール間の強い依存関係による導入失敗と環境汚染を減らすため。
- **Fit Criteria**:
  - 新しい作業ディレクトリから手順を実行し、専用環境内で本体をimportできること。
  - 専用環境内からCOLMAP学習例のコマンドラインヘルプを表示できること。
  - 手順がPython依存とシステム側のGPU driver・CUDA toolkit要件を区別して説明していること。
  - 再現に必要なソース版、Python、PyTorch、CUDA系列を利用者が記録できること。

## 3. 本家追従要件 (Upstream Maintenance Requirements)

### REQ-SYNC-01: 本家更新と自前機能の両立
- **What**: 保守者は本家の開発履歴を維持したまま更新を取り込み、自前機能が失われていないことを統合前に検証できる。
- **Why**: 本家の改善と修正を継続利用しながら、downstream差分の競合と回帰を管理可能にするため。
- **Fit Criteria**:
  - 本家とdownstreamの利用版が別のブランチとして識別できること。
  - 本家更新の統合候補に対して、マスク・損失テストと利用文書の検証結果を確認してから利用版へ統合できること。
  - 本家更新後もマスクなしの標準利用とマスクありの静的シーン利用が選択できること。
  - 検証済み版から、対応する本家ソース版と実行環境を特定できること。

## 4. 非機能要件 (Non-Functional Requirements)

### REQ-NFR-01: 保守性
- **What**: downstream固有機能は責務が明確に分離され、本家更新時の差分を評価しやすい構造で維持されなければならない。
- **Why**: 本家の変更頻度が高い環境でも、自前機能を小さい確認範囲で安全に追従させるため。
- **Fit Criteria**:
  - downstream固有の主要責務境界が設計書に明示されていること。
  - 保守者が単一の標準診断手順を実行すると、本家との共通祖先、downstream固有commit、変更pathを識別できること。
  - 主要機能の回帰観点が自動テストまたは検証手順として確認できること。

### REQ-NFR-02: 検証可能性
- **What**: GPUを必要としない主要回帰は継続的に自動検証され、GPU固有の検証条件は再現可能な形で記録されなければならない。
- **Why**: 日常的な変更を早期に検査しつつ、一般的なホスト型CIでは再現できないCUDA依存の差異を見失わないため。
- **Fit Criteria**:
  - 利用版への変更候補ごとに、マスク・損失テストと文書ビルドの合否を確認できること。
  - GPU検証結果に、ソース版と主要な実行環境の組み合わせが併記されること。

## 5. スコープ定義 (MVP Scope)

- 本MVPは、COLMAP画像列から動的領域を除外した静的シーン3DGSを構築し、その利用環境と本家追従を再現可能に保つことを目的とする。
- 以下を最低保証範囲とする。
  - 画像単位の外部マスクの対応付けと正規化
  - マスクに基づくCOLMAP初期点の選別
  - マスクに基づく画像再構成損失と評価指標の算出
  - `uv` を利用した分離環境へのソースインストール手順
  - 本家ミラーとdownstream利用版を分離したfork運用
  - CPUで実行可能な主要回帰テストと文書検証

## 6. 非目標 (Out of Scope)

- 画像から動的物体マスクを自動生成すること。
- マスクをCOLMAPのカメラ姿勢推定や疎再構築そのものへ適用すること。
- 任意のPython、PyTorch、CUDA、GPU、OSの全組み合わせで動作を保証すること。
- 本家のCUDA rasterizerまたは公開APIをdownstream都合で独自分岐させること。
- dataset、checkpoint、PLY、レンダリング結果をGitリポジトリで配布すること。
